# Do not hoist @types/* to the workspace root: apps/web (React 19) and
# apps/mobile (React Native / React 18) need their own React type versions.
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*
